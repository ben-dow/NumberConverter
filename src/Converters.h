#include <vector>

std::string DecimalToBinaryConverter(int num);

std::string BinaryToDecimalConverter(int num);

void VectorPrinter(std::vector<int> conversion);

std::string ConvertVector(std::vector<int> vec);

